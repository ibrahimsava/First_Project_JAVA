package com.example.demo.Enum;

public enum TypedeTransaction {
    DEPOSIT,     // dépôt (argent qui entre)
    WITHDRAWAL,  // retrait (argent qui sort)
    TRANSFER     // virement vers un autre compte
}
